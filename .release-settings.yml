---
users:
  - lucasilverentand
alpha:
  enabled: true
  version:
    template: "{{version}}-alpha"
  requirements:
    tests: []
  release:
    manual: false
    waitForTracks: []
    platforms:
      - ios
      - web
    allowedUsers:
      - lucasilverentand
beta:
  enabled: false
  version:
    template: "{{version}}-beta"
  requirements:
    tests: []
  release:
    manual: true
    waitForTracks: []
    platforms:
      - ios
      - web
    allowedUsers:
      - lucasilverentand
live:
  enabled: true
  version:
    template: "{{version}}"
  requirements:
    tests: []
  release:
    manual: true
    waitForTracks: []
    platforms:
      - ios
      - web
    allowedUsers:
      - lucasilverentand
